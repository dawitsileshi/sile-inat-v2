# Vercel / production: full URL to the Render backend, e.g.
# VITE_API_URL=https://maternal-wellness-app.onrender.com/api
#
# Local dev: leave unset — Vite proxies /api → 127.0.0.1:5050 (see vite.config.ts)
# VITE_API_URL=
